This is a datascraper for trendpop the tiktok agrigator, which takes an array of links in example.txt, and scrapes the requisit data, inserts it into a preset monday.com account and sends reachout messages from a series of templates via a preset instagram and hotmail account.
